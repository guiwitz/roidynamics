name: splitmask
channels:
  - conda-forge
dependencies:
  - python>=3.8
  - jupyter
  - jupyterlab
  - pip
  - ipympl
  - pip:
    - tqdm
    - git+https://github.com/guiwitz/splitmask.git@master#egg=splitmask
